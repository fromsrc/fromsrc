---
title: Installation
description: Get started with fromsrc
order: 2
---

## Create a new project

The fastest way to start is with create-fromsrc:

<Create />

<Steps>
<Step>

### Run the command

This creates a new directory with everything configured.

```bash
bunx create-fromsrc
```

For non-interactive setup:

```bash
bunx create-fromsrc --name my-docs --framework next.js --yes
```

List available framework values:

```bash
bunx create-fromsrc --list
```

</Step>
<Step>

### Enter the directory

```bash
cd my-docs
```

</Step>
<Step>

### Start the dev server

```bash
bun run dev:up
bun run dev:status
bun run typecheck
```

For log tail and controlled restart:

```bash
bun run dev:tail 80
bun run dev:restart
bun run dev:down
```

</Step>
</Steps>

## Manual installation

Add fromsrc to an existing app:

<Install package="fromsrc" />

<Callout type="info">
  fromsrc adapters are opt-in and tree-shake cleanly.
</Callout>

<Callout type="info">
  For the smallest bundles, prefer subpath imports like `fromsrc/readtime`,
  `fromsrc/searchindex`, and `fromsrc/openapi` for utility-only usage.
</Callout>

For framework-specific walkthroughs, use the manual guides:

<Cards>
  <Card
    title="Manual guides"
    description="Next.js, React Router, TanStack, Vite, Remix, Astro"
    href="/docs/manual"
  />
</Cards>

## Next.js adapter

Use the Next.js adapter entry for framework wiring:

```tsx
import { AdapterProvider, nextAdapter } from "fromsrc/next";

export function Providers({ children }: { children: React.ReactNode }) {
  return <AdapterProvider adapter={nextAdapter}>{children}</AdapterProvider>;
}
```

## React Router adapter

Use the React Router adapter for browser-router apps:

```tsx
import { AdapterProvider, reactRouterAdapter } from "fromsrc/react-router";

export function Providers({ children }: { children: React.ReactNode }) {
  return (
    <AdapterProvider adapter={reactRouterAdapter}>{children}</AdapterProvider>
  );
}
```

## Vite adapter

Use the Vite adapter for custom router stacks:

```tsx
import { AdapterProvider, createAdapter, viteAdapter } from "fromsrc/vite";
import { Link } from "react-router";

const adapter = createAdapter({
  ...viteAdapter,
  Link: ({ href, children }) => <Link to={href}>{children}</Link>,
});

export function Providers({ children }: { children: React.ReactNode }) {
  return <AdapterProvider adapter={adapter}>{children}</AdapterProvider>;
}
```

## TanStack adapter

Use the TanStack adapter for TanStack Router apps:

```tsx
import { AdapterProvider, tanstackAdapter } from "fromsrc/tanstack";

export function Providers({ children }: { children: React.ReactNode }) {
  return (
    <AdapterProvider adapter={tanstackAdapter}>{children}</AdapterProvider>
  );
}
```

## Remix adapter

Use the Remix adapter for Remix routes and navigation:

```tsx
import { AdapterProvider, remixAdapter } from "fromsrc/remix";

export function Providers({ children }: { children: React.ReactNode }) {
  return <AdapterProvider adapter={remixAdapter}>{children}</AdapterProvider>;
}
```

## Astro adapter

Use the Astro adapter for Astro integrations with client-rendered docs surfaces:

```tsx
import { AdapterProvider, astroAdapter } from "fromsrc/astro";

export function Providers({ children }: { children: React.ReactNode }) {
  return <AdapterProvider adapter={astroAdapter}>{children}</AdapterProvider>;
}
```

## Project structure

<Files>
  <Folder name="app" defaultOpen>
    <Folder name="docs">
      <Folder name="[[...slug]]">
        <File name="page.tsx" />
      </Folder>
      <File name="layout.tsx" />
    </Folder>
  </Folder>
  <Folder name="src">
    <File name="providers.tsx" />
  </Folder>
  <Folder name="docs" defaultOpen>
    <File name="index.mdx" />
    <File name="getting-started.mdx" />
  </Folder>
  <File name="package.json" />
</Files>

## Troubleshooting

<Accordion>
  <AccordionItem title="cannot find module 'fromsrc'">
    Ensure fromsrc is installed:

    ```bash
    bun add fromsrc
    ```

  </AccordionItem>
  <AccordionItem title="adapter not resolving">
    Verify the subpath import matches your framework:

    ```tsx
    import { nextAdapter } from "fromsrc/next"
    import { reactRouterAdapter } from "fromsrc/react-router"
    import { viteAdapter } from "fromsrc/vite"
    import { tanstackAdapter } from "fromsrc/tanstack"
    import { remixAdapter } from "fromsrc/remix"
    import { astroAdapter } from "fromsrc/astro"
    import { parseOpenApi } from "fromsrc/openapi"
    import { createAlgoliaAdapter } from "fromsrc/algolia"
    import { createOramaAdapter } from "fromsrc/orama"
    ```

  </AccordionItem>
  <AccordionItem title="bundle size got bigger">
    Run the tree-shake regression check:

    ```bash
    bun run check:shake
    ```

    This validates root, utility, and adapter imports stay inside budget.

  </AccordionItem>
  <AccordionItem title="search not working">
    Search indexes content from your docs folder. Ensure you have `.mdx` files
    with valid frontmatter: ```yaml --- title: page title description: page
    description --- ```
  </AccordionItem>
  <AccordionItem title="styles look wrong">
    Import the fromsrc styles in your root layout or globals.css. Check that
    Tailwind is configured correctly.
  </AccordionItem>
  <AccordionItem title="pages not found">
    Verify your catch-all route is at `app/docs/[[...slug]]/page.tsx` and
    imports from fromsrc correctly.
  </AccordionItem>
  <AccordionItem title="dev process stuck">
    Restart and inspect logs:

    ```bash
    bun run dev:restart
    bun run dev:tail 80
    ```

  </AccordionItem>
</Accordion>

## Next steps

<Cards>
  <Card
    title="Configuration"
    description="Customize fromsrc behavior"
    href="/docs/configuration"
  />
  <Card
    title="Components"
    description="Explore available components"
    href="/docs/components/code"
  />
</Cards>
