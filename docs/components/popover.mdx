---
title: Popover
description: Floating content panel
order: 53
---

Floating panel that appears on click.

## Usage

```tsx
import { Popover } from "fromsrc/client";

<Popover trigger={<button>click me</button>}>
  <p>popover content here</p>
</Popover>;
```

## Alignment

### Start

Default alignment to the left.

```tsx
<Popover align="start" trigger={<button>start</button>}>
  aligned left
</Popover>
```

### Center

Centered below the trigger.

```tsx
<Popover align="center" trigger={<button>center</button>}>
  centered content
</Popover>
```

### End

Aligned to the right.

```tsx
<Popover align="end" trigger={<button>end</button>}>
  aligned right
</Popover>
```

## Side

### Bottom

Default position below the trigger.

```tsx
<Popover side="bottom" trigger={<button>below</button>}>
  appears below
</Popover>
```

### Top

Position above the trigger.

```tsx
<Popover side="top" trigger={<button>above</button>}>
  appears above
</Popover>
```

## Features

- Closes on outside click
- Closes on `Escape` key
- Custom content support

## Props

| prop     | type                         | default  | description                    |
| -------- | ---------------------------- | -------- | ------------------------------ |
| trigger  | ReactNode                    | required | Element that opens the popover |
| children | ReactNode                    | required | Popover content                |
| align    | "start" \| "center" \| "end" | "start"  | Horizontal alignment           |
| side     | "top" \| "bottom"            | "bottom" | Vertical position              |
