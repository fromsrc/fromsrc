---
title: Pagination
description: Page navigation links
order: 37
---

Previous/next navigation between pages.

## Usage

```tsx
<Pagination
  prev={{ href: "/docs/intro", title: "introduction" }}
  next={{ href: "/docs/config", title: "configuration" }}
/>
```

The component renders previous and next links for navigating between documentation pages.

## Only next

```tsx
<Pagination next={{ href: "/docs/quickstart", title: "quickstart" }} />
```

## Only previous

```tsx
<Pagination prev={{ href: "/docs/installation", title: "installation" }} />
```

## Props

| prop | type   | description        |
| ---- | ------ | ------------------ |
| prev | object | Previous page link |
| next | object | Next page link     |

### Link object

| prop  | type   | description |
| ----- | ------ | ----------- |
| href  | string | Page URL    |
| title | string | Page title  |

## Styling

The component uses theme tokens for consistent styling:

- `border-line` for borders
- `bg-surface/30` for background with hover state
- `text-muted` and `text-fg` for text colors

Returns `null` when both prev and next are undefined.
