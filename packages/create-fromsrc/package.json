{
	"name": "create-fromsrc",
	"version": "0.0.0",
	"description": "create a new fromsrc documentation site",
	"type": "module",
	"bin": "./dist/bin.js",
	"files": ["dist", "template"],
	"scripts": {
		"build": "tsup src/bin.ts --format esm --dts",
		"dev": "tsup src/bin.ts --format esm --dts --watch"
	},
	"dependencies": {
		"@clack/prompts": "^0.11.0",
		"commander": "^14.0.0",
		"picocolors": "^1.1.1"
	},
	"devDependencies": {
		"tsup": "^8.5.0",
		"typescript": "^5.9.0"
	}
}
